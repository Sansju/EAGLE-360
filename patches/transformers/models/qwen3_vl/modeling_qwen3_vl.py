#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
#           This file was automatically generated from src/transformers/models/qwen3_vl/modular_qwen3_vl.py.
#               Do NOT edit this file manually as any edits will be overwritten by the generation of
#             the file from the modular. If any change should be done, please apply the change to the
#                          modular_qwen3_vl.py file directly. One of our CI enforces this.
#                🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨🚨
# coding=utf-8
# Copyright 2025 The Qwen Team and The HuggingFace Inc. team. All rights reserved.
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

from dataclasses import dataclass
from typing import Any, Callable, Optional, Union

import torch
import torch.nn as nn
import torch.nn.functional as F

from ...activations import ACT2FN
from ...cache_utils import Cache, DynamicCache
from ...generation import GenerationMixin
from ...integrations import use_kernel_forward_from_hub
from ...masking_utils import create_causal_mask
from ...modeling_flash_attention_utils import FlashAttentionKwargs
from ...modeling_layers import GradientCheckpointingLayer
from ...modeling_outputs import BaseModelOutputWithPast, ModelOutput
from ...modeling_rope_utils import ROPE_INIT_FUNCTIONS, dynamic_rope_update
from ...modeling_utils import ALL_ATTENTION_FUNCTIONS, PreTrainedModel
from ...processing_utils import Unpack
from ...utils import TransformersKwargs, auto_docstring, is_torchdynamo_compiling
from ...utils.deprecation import deprecate_kwarg
from ...utils.generic import check_model_inputs
from .configuration_qwen3_vl import Qwen3VLConfig, Qwen3VLTextConfig, Qwen3VLVisionConfig


class Qwen3VLVisionMLP(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.hidden_size = config.hidden_size
        self.intermediate_size = config.intermediate_size
        self.linear_fc1 = nn.Linear(self.hidden_size, self.intermediate_size, bias=True)
        self.linear_fc2 = nn.Linear(self.intermediate_size, self.hidden_size, bias=True)
        self.act_fn = ACT2FN[config.hidden_act]

    def forward(self, hidden_state):
        return self.linear_fc2(self.act_fn(self.linear_fc1(hidden_state)))


class Qwen3VLVisionPatchEmbed(nn.Module):
    def __init__(self, config) -> None:
        super().__init__()
        self.patch_size = config.patch_size
        self.temporal_patch_size = config.temporal_patch_size
        self.in_channels = config.in_channels
        self.embed_dim = config.hidden_size

        kernel_size = [self.temporal_patch_size, self.patch_size, self.patch_size]
        self.proj = nn.Conv3d(self.in_channels, self.embed_dim, kernel_size=kernel_size, stride=kernel_size, bias=True)

    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        target_dtype = self.proj.weight.dtype
        hidden_states = hidden_states.view(
            -1, self.in_channels, self.temporal_patch_size, self.patch_size, self.patch_size
        )
        hidden_states = self.proj(hidden_states.to(dtype=target_dtype)).view(-1, self.embed_dim)
        return hidden_states


class Qwen3VLVisionRotaryEmbedding(nn.Module):
    inv_freq: torch.Tensor  # fix linting for `register_buffer`

    def __init__(self, dim: int, theta: float = 10000.0) -> None:
        super().__init__()
        inv_freq = 1.0 / (theta ** (torch.arange(0, dim, 2, dtype=torch.float) / dim))
        self.register_buffer("inv_freq", inv_freq, persistent=False)

    def forward(self, seqlen: int) -> torch.Tensor:
        seq = torch.arange(seqlen, device=self.inv_freq.device, dtype=self.inv_freq.dtype)
        freqs = torch.outer(seq, self.inv_freq)
        return freqs


class Qwen3VLVisionPatchMerger(nn.Module):
    def __init__(self, config: Qwen3VLVisionConfig, use_postshuffle_norm=False) -> None:
        super().__init__()
        self.hidden_size = config.hidden_size * (config.spatial_merge_size**2)
        self.use_postshuffle_norm = use_postshuffle_norm
        self.norm = nn.LayerNorm(self.hidden_size if use_postshuffle_norm else config.hidden_size, eps=1e-6)
        self.linear_fc1 = nn.Linear(self.hidden_size, self.hidden_size)
        self.act_fn = nn.GELU()
        self.linear_fc2 = nn.Linear(self.hidden_size, config.out_hidden_size)

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        x = self.norm(x.view(-1, self.hidden_size) if self.use_postshuffle_norm else x).view(-1, self.hidden_size)
        x = self.linear_fc2(self.act_fn(self.linear_fc1(x)))
        return x


def rotate_half(x):
    """Rotates half the hidden dims of the input."""
    x1 = x[..., : x.shape[-1] // 2]
    x2 = x[..., x.shape[-1] // 2 :]
    return torch.cat((-x2, x1), dim=-1)


def apply_rotary_pos_emb_vision(
    q: torch.Tensor, k: torch.Tensor, cos: torch.Tensor, sin: torch.Tensor
) -> tuple[torch.Tensor, torch.Tensor]:
    orig_q_dtype = q.dtype
    orig_k_dtype = k.dtype
    q, k = q.float(), k.float()
    cos, sin = cos.unsqueeze(-2).float(), sin.unsqueeze(-2).float()
    q_embed = (q * cos) + (rotate_half(q) * sin)
    k_embed = (k * cos) + (rotate_half(k) * sin)
    q_embed = q_embed.to(orig_q_dtype)
    k_embed = k_embed.to(orig_k_dtype)
    return q_embed, k_embed


def repeat_kv(hidden_states: torch.Tensor, n_rep: int) -> torch.Tensor:
    """
    This is the equivalent of torch.repeat_interleave(x, dim=1, repeats=n_rep). The hidden states go from (batch,
    num_key_value_heads, seqlen, head_dim) to (batch, num_attention_heads, seqlen, head_dim)
    """
    batch, num_key_value_heads, slen, head_dim = hidden_states.shape
    if n_rep == 1:
        return hidden_states
    hidden_states = hidden_states[:, :, None, :, :].expand(batch, num_key_value_heads, n_rep, slen, head_dim)
    return hidden_states.reshape(batch, num_key_value_heads * n_rep, slen, head_dim)


def eager_attention_forward(
    module: nn.Module,
    query: torch.Tensor,
    key: torch.Tensor,
    value: torch.Tensor,
    attention_mask: Optional[torch.Tensor],
    scaling: float,
    dropout: float = 0.0,
    **kwargs: Unpack[TransformersKwargs],
):
    key_states = repeat_kv(key, module.num_key_value_groups)
    value_states = repeat_kv(value, module.num_key_value_groups)

    attn_weights = torch.matmul(query, key_states.transpose(2, 3)) * scaling
    if attention_mask is not None:
        causal_mask = attention_mask[:, :, :, : key_states.shape[-2]]
        attn_weights = attn_weights + causal_mask

    attn_weights = nn.functional.softmax(attn_weights, dim=-1, dtype=torch.float32).to(query.dtype)
    attn_weights = nn.functional.dropout(attn_weights, p=dropout, training=module.training)
    attn_output = torch.matmul(attn_weights, value_states)
    attn_output = attn_output.transpose(1, 2).contiguous()

    return attn_output, attn_weights


class Qwen3VLVisionAttention(nn.Module):
    def __init__(self, config: Qwen3VLVisionConfig) -> None:
        super().__init__()
        self.dim = config.hidden_size
        self.num_heads = config.num_heads
        self.head_dim = self.dim // self.num_heads
        self.num_key_value_groups = 1  # needed for eager attention
        self.qkv = nn.Linear(self.dim, self.dim * 3, bias=True)
        self.proj = nn.Linear(self.dim, self.dim)
        self.scaling = self.head_dim**-0.5
        self.config = config
        self.attention_dropout = 0.0
        self.is_causal = False

    def forward(
        self,
        hidden_states: torch.Tensor,
        cu_seqlens: torch.Tensor,
        rotary_pos_emb: Optional[torch.Tensor] = None,
        position_embeddings: Optional[tuple[torch.Tensor, torch.Tensor]] = None,
        **kwargs,
    ) -> torch.Tensor:
        seq_length = hidden_states.shape[0]
        query_states, key_states, value_states = (
            self.qkv(hidden_states).reshape(seq_length, 3, self.num_heads, -1).permute(1, 0, 2, 3).unbind(0)
        )
        cos, sin = position_embeddings
        query_states, key_states = apply_rotary_pos_emb_vision(query_states, key_states, cos, sin)

        query_states = query_states.transpose(0, 1).unsqueeze(0)
        key_states = key_states.transpose(0, 1).unsqueeze(0)
        value_states = value_states.transpose(0, 1).unsqueeze(0)

        attention_interface: Callable = eager_attention_forward
        if self.config._attn_implementation != "eager":
            attention_interface = ALL_ATTENTION_FUNCTIONS[self.config._attn_implementation]

        if self.config._attn_implementation == "flash_attention_2":
            # Flash Attention 2: Use cu_seqlens for variable length attention
            max_seqlen = (cu_seqlens[1:] - cu_seqlens[:-1]).max()
            attn_output, _ = attention_interface(
                self,
                query_states,
                key_states,
                value_states,
                attention_mask=None,
                scaling=self.scaling,
                dropout=0.0 if not self.training else self.attention_dropout,
                cu_seq_lens_q=cu_seqlens,
                cu_seq_lens_k=cu_seqlens,
                max_length_q=max_seqlen,
                max_length_k=max_seqlen,
                is_causal=False,
                **kwargs,
            )
        else:
            # Other implementations: Process each chunk separately
            lengths = cu_seqlens[1:] - cu_seqlens[:-1]
            splits = [
                torch.split(tensor, lengths.tolist(), dim=2) for tensor in (query_states, key_states, value_states)
            ]

            attn_outputs = [
                attention_interface(
                    self,
                    q,
                    k,
                    v,
                    attention_mask=None,
                    scaling=self.scaling,
                    dropout=0.0 if not self.training else self.attention_dropout,
                    is_causal=False,
                    **kwargs,
                )[0]
                for q, k, v in zip(*splits)
            ]
            attn_output = torch.cat(attn_outputs, dim=1)

        attn_output = attn_output.reshape(seq_length, -1).contiguous()
        attn_output = self.proj(attn_output)
        return attn_output


class Qwen3VLVisionBlock(GradientCheckpointingLayer):
    def __init__(self, config, attn_implementation: str = "sdpa") -> None:
        super().__init__()
        self.norm1 = nn.LayerNorm(config.hidden_size, eps=1e-6)
        self.norm2 = nn.LayerNorm(config.hidden_size, eps=1e-6)
        self.attn = Qwen3VLVisionAttention(config=config)
        self.mlp = Qwen3VLVisionMLP(config=config)

    def forward(
        self,
        hidden_states: torch.Tensor,
        cu_seqlens: torch.Tensor,
        rotary_pos_emb: Optional[torch.Tensor] = None,
        position_embeddings: Optional[tuple[torch.Tensor, torch.Tensor]] = None,
        **kwargs,
    ) -> torch.Tensor:
        hidden_states = hidden_states + self.attn(
            self.norm1(hidden_states),
            cu_seqlens=cu_seqlens,
            rotary_pos_emb=rotary_pos_emb,
            position_embeddings=position_embeddings,
            **kwargs,
        )
        hidden_states = hidden_states + self.mlp(self.norm2(hidden_states))
        return hidden_states


class Qwen3VLTextRotaryEmbedding(nn.Module):
    inv_freq: torch.Tensor  # fix linting for `register_buffer`

    def __init__(self, config: Qwen3VLTextConfig, device=None):
        super().__init__()
        if hasattr(config, "rope_scaling") and config.rope_scaling is not None:
            self.rope_type = config.rope_scaling.get("rope_type", "default")
        else:
            self.rope_type = "default"
        self.max_seq_len_cached = config.max_position_embeddings
        self.original_max_seq_len = config.max_position_embeddings

        self.config = config
        self.rope_init_fn = ROPE_INIT_FUNCTIONS[self.rope_type]

        inv_freq, self.attention_scaling = self.rope_init_fn(self.config, device)
        self.register_buffer("inv_freq", inv_freq, persistent=False)
        self.original_inv_freq = self.inv_freq

        self.mrope_section = config.rope_scaling.get("mrope_section", [24, 20, 20])

    def apply_interleaved_mrope(self, freqs, mrope_section):
        """Apply interleaved MRoPE to 3D rotary embeddings.
        Reorganizes frequency layout from chunked [TTT...HHH...WWW] to
        interleaved [THTHWHTHW...TT], preserving frequency continuity.
        args:
            x: (3, bs, seq_len, head_dim // 2)
            mrope_section: (3,)
        returns:
            x_t: (bs, seq_len, head_dim // 2)
        """
        freqs_t = freqs[0]  # just overwrite the first dimension T
        for dim, offset in enumerate((1, 2), start=1):  # H, W
            length = mrope_section[dim] * 3
            idx = slice(offset, length, 3)
            freqs_t[..., idx] = freqs[dim, ..., idx]
        return freqs_t

    @torch.no_grad()
    @dynamic_rope_update  # power user: used with advanced RoPE types (e.g. dynamic rope)
    def forward(self, x, position_ids):
        # In contrast to other models, Qwen3VL has different position ids for the grids
        # So we expand the inv_freq to shape (3, ...)
        if position_ids.ndim == 2:
            position_ids = position_ids[None, ...].expand(3, position_ids.shape[0], -1)
        inv_freq_expanded = self.inv_freq[None, None, :, None].float().expand(3, position_ids.shape[1], -1, 1)
        position_ids_expanded = position_ids[:, :, None, :].float()  # shape (3, bs, 1, positions)

        device_type = x.device.type if isinstance(x.device.type, str) and x.device.type != "mps" else "cpu"
        with torch.autocast(device_type=device_type, enabled=False):  # Force float32
            freqs = (inv_freq_expanded.float() @ position_ids_expanded.float()).transpose(2, 3)
            freqs = self.apply_interleaved_mrope(freqs, self.mrope_section)
            emb = torch.cat((freqs, freqs), dim=-1)
            cos = emb.cos() * self.attention_scaling
            sin = emb.sin() * self.attention_scaling

        return cos.to(dtype=x.dtype), sin.to(dtype=x.dtype)


@use_kernel_forward_from_hub("RMSNorm")
class Qwen3VLTextRMSNorm(nn.Module):
    def __init__(self, hidden_size, eps: float = 1e-6) -> None:
        """
        Qwen3VLTextRMSNorm is equivalent to T5LayerNorm
        """
        super().__init__()
        self.weight = nn.Parameter(torch.ones(hidden_size))
        self.variance_epsilon = eps

    def forward(self, hidden_states: torch.Tensor) -> torch.Tensor:
        input_dtype = hidden_states.dtype
        hidden_states = hidden_states.to(torch.float32)
        variance = hidden_states.pow(2).mean(-1, keepdim=True)
        hidden_states = hidden_states * torch.rsqrt(variance + self.variance_epsilon)
        return self.weight * hidden_states.to(input_dtype)

    def extra_repr(self):
        return f"{tuple(self.weight.shape)}, eps={self.variance_epsilon}"


def apply_rotary_pos_emb(q, k, cos, sin, position_ids=None, unsqueeze_dim=1):
    """Applies Rotary Position Embedding to the query and key tensors.

    Args:
        q (`torch.Tensor`): The query tensor.
        k (`torch.Tensor`): The key tensor.
        cos (`torch.Tensor`): The cosine part of the rotary embedding.
        sin (`torch.Tensor`): The sine part of the rotary embedding.
        position_ids (`torch.Tensor`, *optional*):
            Deprecated and unused.
        unsqueeze_dim (`int`, *optional*, defaults to 1):
            The 'unsqueeze_dim' argument specifies the dimension along which to unsqueeze cos[position_ids] and
            sin[position_ids] so that they can be properly broadcasted to the dimensions of q and k. For example, note
            that cos[position_ids] and sin[position_ids] have the shape [batch_size, seq_len, head_dim]. Then, if q and
            k have the shape [batch_size, heads, seq_len, head_dim], then setting unsqueeze_dim=1 makes
            cos[position_ids] and sin[position_ids] broadcastable to the shapes of q and k. Similarly, if q and k have
            the shape [batch_size, seq_len, heads, head_dim], then set unsqueeze_dim=2.
    Returns:
        `tuple(torch.Tensor)` comprising of the query and key tensors rotated using the Rotary Position Embedding.
    """
    cos = cos.unsqueeze(unsqueeze_dim)
    sin = sin.unsqueeze(unsqueeze_dim)
    q_embed = (q * cos) + (rotate_half(q) * sin)
    k_embed = (k * cos) + (rotate_half(k) * sin)
    return q_embed, k_embed


def create_panoramic_rotated_mask(
    attention_mask: Optional[torch.Tensor],
    panoramic_mask: torch.Tensor,
    panoramic_widths: torch.Tensor,
    position_ids: torch.Tensor,
    num_heads: int,
    window_size: int = 32,
) -> Optional[torch.Tensor]:
    """
    为全景图创建旋转对齐的 per-head 注意力掩码。
    
    每个注意力头会看到"旋转"后的全景图，对应的注意力掩码也会旋转，
    使得跨边界的token能够建立连接。
    
    Args:
        attention_mask: 原始掩码，shape (batch_size, 1, seq_len, seq_len)，可以为None
        panoramic_mask: 标记哪些位置是全景图，shape (batch_size, seq_len)
        panoramic_widths: 每个位置对应的全景图宽度，shape (batch_size, seq_len)
        position_ids: 3D位置编码 (3, batch_size, seq_len)，其中 position_ids[2] 是 w 坐标
        num_heads: 注意力头数量
        window_size: 注意力窗口大小（用于边界连接）
        
    Returns:
        per_head_mask: shape (batch_size, num_heads, seq_len, seq_len)，如果输入为None则返回None
    """
    # 如果attention_mask为None，需要根据panoramic_mask创建基础掩码
    if attention_mask is None:
        # 从panoramic_mask推断batch_size和seq_len
        batch_size, seq_len = panoramic_mask.shape
        device = panoramic_mask.device
        
        # 如果没有全景图位置，返回None（保持原始行为）
        if not panoramic_mask.any():
            return None
        
        # 创建一个基础掩码：全部允许连接（0），然后我们会为全景图部分设置特殊的旋转连接
        # 注意：这里我们创建的是 (batch, 1, seq, seq) 格式
        attention_mask = torch.zeros(batch_size, 1, seq_len, seq_len, device=device, dtype=torch.float32)
    else:
        batch_size, _, seq_len, _ = attention_mask.shape
        device = attention_mask.device
    
    # 如果没有全景图位置，直接返回原始掩码
    if not panoramic_mask.any():
        return attention_mask.expand(batch_size, num_heads, seq_len, seq_len)
    
    # 扩展为 per-head 掩码
    per_head_mask = attention_mask.expand(batch_size, num_heads, seq_len, seq_len).clone()
    
    # 确保 position_ids 是 3D 的
    if position_ids.ndim == 2:
        position_ids = position_ids[None, ...].expand(3, batch_size, seq_len)
    
    # 向量化处理：为每个batch处理
    for b in range(batch_size):
        pano_positions = torch.where(panoramic_mask[b])[0]
        if len(pano_positions) == 0:
            continue
        
        # 获取全景图token的W值和h值（用于区分不同的全景图）
        W_values = panoramic_widths[b, pano_positions]
        w_coords = position_ids[2, b, pano_positions]
        h_coords = position_ids[1, b, pano_positions]
        
        # 过滤掉无效的W值
        valid_mask = W_values > 0
        if not valid_mask.any():
            continue
        
        pano_positions = pano_positions[valid_mask]
        W_values = W_values[valid_mask]  # (num_pano,)
        w_coords = w_coords[valid_mask]  # (num_pano,)
        h_coords = h_coords[valid_mask]  # (num_pano,)
        
        num_pano = len(pano_positions)
        
        # === 向量化计算：为所有头一次性处理 ===
        
        # 1. 先重置全景图区域的掩码为 -inf
        # per_head_mask[b, :, pano_positions[:, None], pano_positions[None, :]] = float('-inf')
        # 使用更高效的索引方式
        pano_idx = pano_positions.unsqueeze(0).expand(num_pano, -1)  # (num_pano, num_pano)
        per_head_mask[b, :, pano_positions, :][:, :, pano_positions] = float('-inf')
        
        # 2. 构建配对矩阵：所有位置对的信息
        # W_i, W_j: (num_pano, num_pano) - 每对位置的宽度
        W_i = W_values.unsqueeze(1).expand(-1, num_pano)  # (num_pano, num_pano)
        W_j = W_values.unsqueeze(0).expand(num_pano, -1)  # (num_pano, num_pano)
        
        # w_i, w_j: (num_pano, num_pano) - 每对位置的w坐标
        w_i = w_coords.unsqueeze(1).expand(-1, num_pano).float()
        w_j = w_coords.unsqueeze(0).expand(num_pano, -1).float()
        
        # h_i, h_j: (num_pano, num_pano) - 每对位置的h坐标
        h_i = h_coords.unsqueeze(1).expand(-1, num_pano).float()
        h_j = h_coords.unsqueeze(0).expand(num_pano, -1).float()
        
        # 3. 计算同一全景图的掩码
        # 条件1: W值相同
        same_width = (W_i == W_j)  # (num_pano, num_pano)
        # 条件2: h坐标差异不大（同一张图）
        h_diff = torch.abs(h_i - h_j)
        same_image = same_width & (h_diff <= W_i.float())  # (num_pano, num_pano)
        
        # 4. 计算原始的相对w距离（未旋转）
        w_diff = (w_j - w_i) % W_i.float()  # (num_pano, num_pano)
        
        # 5. 为所有头计算旋转偏移和掩码
        # head_ratios: (num_heads, 1, 1)
        head_ratios = torch.arange(num_heads, device=device, dtype=torch.float32).view(num_heads, 1, 1) / num_heads
        
        # rotation_offsets: (num_heads, num_pano, num_pano)
        rotation_offsets = (W_i.unsqueeze(0) * head_ratios).long()  # (num_heads, num_pano, num_pano)
        
        # 6. 计算旋转后的相对位置
        # rotated_relative_pos: (num_heads, num_pano, num_pano)
        rotated_relative_pos = (w_diff.unsqueeze(0) - rotation_offsets.float()) % W_i.unsqueeze(0).float()
        
        # 7. 计算环形距离
        dist_forward = rotated_relative_pos
        dist_backward = W_i.unsqueeze(0).float() - rotated_relative_pos
        circular_dist = torch.minimum(dist_forward, dist_backward)  # (num_heads, num_pano, num_pano)
        
        # 8. 判断是否在窗口范围内
        in_window = circular_dist <= window_size  # (num_heads, num_pano, num_pano)
        
        # 9. 对角线（自己看自己）总是允许
        diagonal_mask = torch.eye(num_pano, device=device, dtype=torch.bool).unsqueeze(0)  # (1, num_pano, num_pano)
        
        # 10. 组合所有条件
        # 需要满足：同一张图 & (在窗口内 或 是自己)
        valid_connections = same_image.unsqueeze(0) & (in_window | diagonal_mask)  # (num_heads, num_pano, num_pano)
        
        # 11. 应用掩码：valid_connections 为 True 的位置设为 0.0
        # 使用高级索引一次性更新
        for h in range(num_heads):
            valid_pairs = valid_connections[h]  # (num_pano, num_pano)
            if valid_pairs.any():
                # 找到所有有效连接的位置
                i_indices, j_indices = torch.where(valid_pairs)
                # 转换到原始序列中的索引
                orig_i = pano_positions[i_indices]
                orig_j = pano_positions[j_indices]
                # 批量更新
                per_head_mask[b, h, orig_i, orig_j] = 0.0
    
    return per_head_mask


class Qwen3VLTextAttention(nn.Module):
    """Multi-headed attention from 'Attention Is All You Need' paper"""

    def __init__(self, config: Qwen3VLTextConfig, layer_idx: int):
        super().__init__()
        self.config = config
        self.layer_idx = layer_idx
        self.head_dim = getattr(config, "head_dim", config.hidden_size // config.num_attention_heads)
        self.num_key_value_groups = config.num_attention_heads // config.num_key_value_heads
        self.scaling = self.head_dim**-0.5
        self.attention_dropout = config.attention_dropout
        self.is_causal = True

        self.q_proj = nn.Linear(
            config.hidden_size, config.num_attention_heads * self.head_dim, bias=config.attention_bias
        )
        self.k_proj = nn.Linear(
            config.hidden_size, config.num_key_value_heads * self.head_dim, bias=config.attention_bias
        )
        self.v_proj = nn.Linear(
            config.hidden_size, config.num_key_value_heads * self.head_dim, bias=config.attention_bias
        )
        self.o_proj = nn.Linear(
            config.num_attention_heads * self.head_dim, config.hidden_size, bias=config.attention_bias
        )
        self.q_norm = Qwen3VLTextRMSNorm(self.head_dim, eps=config.rms_norm_eps)  # unlike olmo, only on the head dim!
        self.k_norm = Qwen3VLTextRMSNorm(
            self.head_dim, eps=config.rms_norm_eps
        )  # thus post q_norm does not need reshape

    @deprecate_kwarg("past_key_value", new_name="past_key_values", version="4.58")
    def forward(
        self,
        hidden_states: torch.Tensor,
        position_embeddings: tuple[torch.Tensor, torch.Tensor],
        attention_mask: Optional[torch.Tensor],
        past_key_values: Optional[Cache] = None,
        cache_position: Optional[torch.LongTensor] = None,
        panoramic_mask: Optional[torch.Tensor] = None,
        panoramic_widths: Optional[torch.Tensor] = None,
        position_ids: Optional[torch.LongTensor] = None,
        **kwargs: Unpack[FlashAttentionKwargs],
    ) -> tuple[torch.Tensor, Optional[torch.Tensor]]:
        input_shape = hidden_states.shape[:-1]
        hidden_shape = (*input_shape, -1, self.head_dim)

        query_states = self.q_norm(self.q_proj(hidden_states).view(hidden_shape)).transpose(1, 2)
        key_states = self.k_norm(self.k_proj(hidden_states).view(hidden_shape)).transpose(1, 2)
        value_states = self.v_proj(hidden_states).view(hidden_shape).transpose(1, 2)

        cos, sin = position_embeddings
        
        # Apply multi-head rolling for panoramic images
        if panoramic_mask is not None and panoramic_widths is not None and panoramic_mask.any() and position_ids is not None:
            # For panoramic positions, apply per-head rolling offset
            # Formula: 第m个注意力头的w坐标应该为 (pw + W*m/M) mod W
            # Optimized version: vectorized computation to avoid nested loops
            batch_size, num_query_heads, seq_len, head_dim = query_states.shape
            num_key_value_heads = key_states.shape[1]
            
            # Expand position_ids to shape (3, batch_size, seq_len) if needed
            if position_ids.ndim == 2:
                position_ids = position_ids[None, ...].expand(3, position_ids.shape[0], -1)
            
            # Get rotary embedding parameters
            if hasattr(self.config, 'rope_scaling') and self.config.rope_scaling:
                mrope_section = self.config.rope_scaling.get("mrope_section", [24, 20, 20])
            else:
                mrope_section = [24, 20, 20]
            
            w_section_len = mrope_section[2]
            rope_theta = getattr(self.config, 'rope_theta', 10000.0)
            
            # Precompute frequency indices for w-dimension
            # freq_idx = 2, 5, 8, 11, ... (2 + w_idx * 3)
            freq_indices = torch.arange(w_section_len, device=cos.device) * 3 + 2
            freq_indices = freq_indices[freq_indices < head_dim]
            actual_w_section_len = len(freq_indices)
            
            if actual_w_section_len > 0:
                # Precompute theta values for all frequencies: shape (actual_w_section_len,)
                w_indices = torch.arange(actual_w_section_len, device=cos.device, dtype=cos.dtype)
                theta_values = rope_theta ** (-2.0 * w_indices / (w_section_len * 2))
                
                # Create per-head cos/sin tensors
                # Query: (batch_size, num_query_heads, seq_len, head_dim)
                cos_per_head_q = cos.unsqueeze(1).expand(batch_size, num_query_heads, seq_len, head_dim).clone()
                sin_per_head_q = sin.unsqueeze(1).expand(batch_size, num_query_heads, seq_len, head_dim).clone()
                
                # Key: (batch_size, num_key_value_heads, seq_len, head_dim)
                cos_per_head_k = cos.unsqueeze(1).expand(batch_size, num_key_value_heads, seq_len, head_dim).clone()
                sin_per_head_k = sin.unsqueeze(1).expand(batch_size, num_key_value_heads, seq_len, head_dim).clone()
                
                # Process query heads with vectorized operations
                # Shape: (num_query_heads, 1) - head offset ratios
                head_ratios_q = torch.arange(num_query_heads, device=cos.device, dtype=cos.dtype).unsqueeze(1) / num_query_heads
                
                # For each batch, process all heads and positions together
                for b in range(batch_size):
                    pano_positions = torch.where(panoramic_mask[b])[0]
                    if len(pano_positions) == 0:
                        continue
                    
                    # Get W and original_w for all panoramic positions: (num_pano_pos,)
                    # Keep same dtype as cos/sin to avoid dtype mismatch
                    W_values = panoramic_widths[b, pano_positions].to(cos.dtype)  # (num_pano_pos,)
                    original_w_values = position_ids[2, b, pano_positions].to(cos.dtype)  # (num_pano_pos,)
                    
                    # Filter out positions with W <= 0
                    valid_mask = W_values > 0
                    if not valid_mask.any():
                        continue
                    
                    pano_positions = pano_positions[valid_mask]
                    W_values = W_values[valid_mask]  # (num_valid_pos,)
                    original_w_values = original_w_values[valid_mask]  # (num_valid_pos,)
                    
                    # Compute w_delta for all query heads and positions
                    # offset: (num_query_heads, num_valid_pos)
                    offset_q = head_ratios_q * W_values.unsqueeze(0)
                    new_w_q = (original_w_values.unsqueeze(0) + offset_q) % W_values.unsqueeze(0)
                    w_delta_q = new_w_q - original_w_values.unsqueeze(0)  # (num_query_heads, num_valid_pos)
                    
                    # Compute phase shifts for all frequencies
                    # phase_shift: (num_query_heads, num_valid_pos, actual_w_section_len)
                    phase_shift_q = w_delta_q.unsqueeze(2) * theta_values.unsqueeze(0).unsqueeze(0)
                    
                    # Extract cos/sin values for w-dimension frequencies
                    # Shape: (num_query_heads, num_valid_pos, actual_w_section_len)
                    cos_vals_q = cos_per_head_q[b, :, pano_positions, :][:, :, freq_indices]
                    sin_vals_q = sin_per_head_q[b, :, pano_positions, :][:, :, freq_indices]
                    
                    # Compute rotation: cos(α+β) = cos(α)cos(β) - sin(α)sin(β)
                    # Convert to same dtype as cos_per_head_q to avoid dtype mismatch
                    cos_shift_q = torch.cos(phase_shift_q).to(cos_per_head_q.dtype)
                    sin_shift_q = torch.sin(phase_shift_q).to(cos_per_head_q.dtype)
                    
                    new_cos_q = cos_vals_q * cos_shift_q - sin_vals_q * sin_shift_q
                    new_sin_q = sin_vals_q * cos_shift_q + cos_vals_q * sin_shift_q
                    
                    # Write back the results
                    cos_per_head_q[b, :, pano_positions, :][:, :, freq_indices] = new_cos_q
                    sin_per_head_q[b, :, pano_positions, :][:, :, freq_indices] = new_sin_q
                    
                    # Process key heads similarly
                    head_ratios_k = torch.arange(num_key_value_heads, device=cos.device, dtype=cos.dtype).unsqueeze(1) / num_key_value_heads
                    
                    offset_k = head_ratios_k * W_values.unsqueeze(0)
                    new_w_k = (original_w_values.unsqueeze(0) + offset_k) % W_values.unsqueeze(0)
                    w_delta_k = new_w_k - original_w_values.unsqueeze(0)  # (num_key_value_heads, num_valid_pos)
                    
                    # phase_shift: (num_key_value_heads, num_valid_pos, actual_w_section_len)
                    phase_shift_k = w_delta_k.unsqueeze(2) * theta_values.unsqueeze(0).unsqueeze(0)
                    
                    cos_vals_k = cos_per_head_k[b, :, pano_positions, :][:, :, freq_indices]
                    sin_vals_k = sin_per_head_k[b, :, pano_positions, :][:, :, freq_indices]
                    
                    # Convert to same dtype as cos_per_head_k to avoid dtype mismatch
                    cos_shift_k = torch.cos(phase_shift_k).to(cos_per_head_k.dtype)
                    sin_shift_k = torch.sin(phase_shift_k).to(cos_per_head_k.dtype)
                    
                    new_cos_k = cos_vals_k * cos_shift_k - sin_vals_k * sin_shift_k
                    new_sin_k = sin_vals_k * cos_shift_k + cos_vals_k * sin_shift_k
                    
                    cos_per_head_k[b, :, pano_positions, :][:, :, freq_indices] = new_cos_k
                    sin_per_head_k[b, :, pano_positions, :][:, :, freq_indices] = new_sin_k
                
                # Apply per-head RoPE
                query_states = (query_states * cos_per_head_q) + (rotate_half(query_states) * sin_per_head_q)
                key_states = (key_states * cos_per_head_k) + (rotate_half(key_states) * sin_per_head_k)
            else:
                # No valid frequency indices, use standard RoPE
                query_states = (query_states * cos) + (rotate_half(query_states) * sin)
                key_states = (key_states * cos) + (rotate_half(key_states) * sin)
        else:
            # Standard RoPE application
            query_states, key_states = apply_rotary_pos_emb(query_states, key_states, cos, sin)

        if past_key_values is not None:
            # sin and cos are specific to RoPE models; cache_position needed for the static cache
            cache_kwargs = {"sin": sin, "cos": cos, "cache_position": cache_position}
            key_states, value_states = past_key_values.update(key_states, value_states, self.layer_idx, cache_kwargs)

        # Apply per-head rotated mask for panoramic images
        # This creates toroidal (circular) attention patterns that align with the rolling RoPE
        # NOTE: Rotated mask requires eager attention, as Flash Attention 2 doesn't support custom masks
        use_rotated_mask = False
        if panoramic_mask is not None and panoramic_widths is not None and panoramic_mask.any() and position_ids is not None:
            batch_size, num_query_heads, seq_len, head_dim = query_states.shape
            
            # Check if we should use rotated mask (requires eager attention)
            # Flash Attention doesn't support arbitrary per-head masks
            if self.config._attn_implementation == "eager":
                # Generate per-head rotated attention mask
                # Each head sees a "rotated" view of the panorama, and its attention pattern is also rotated
                attention_mask = create_panoramic_rotated_mask(
                    attention_mask=attention_mask,
                    panoramic_mask=panoramic_mask,
                    panoramic_widths=panoramic_widths,
                    position_ids=position_ids,
                    num_heads=num_query_heads,
                    window_size=32,  # Adjust based on your needs
                )
                use_rotated_mask = True
            else:
                # Flash Attention or SDPA: can't use per-head rotated masks
                # Fall back to Rolling RoPE only (still provides rotation awareness via position encoding)
                # Note: You can force eager attention by setting _attn_implementation="eager" in config
                pass

        attention_interface: Callable = eager_attention_forward
        if self.config._attn_implementation != "eager":
            attention_interface = ALL_ATTENTION_FUNCTIONS[self.config._attn_implementation]

        attn_output, attn_weights = attention_interface(
            self,
            query_states,
            key_states,
            value_states,
            attention_mask,
            dropout=0.0 if not self.training else self.attention_dropout,
            scaling=self.scaling,
            **kwargs,
        )

        attn_output = attn_output.reshape(*input_shape, -1).contiguous()
        attn_output = self.o_proj(attn_output)
        return attn_output, attn_weights


class Qwen3VLTextMLP(nn.Module):
    def __init__(self, config):
        super().__init__()
        self.config = config
        self.hidden_size = config.hidden_size
        self.intermediate_size = config.intermediate_size
        self.gate_proj = nn.Linear(self.hidden_size, self.intermediate_size, bias=False)
        self.up_proj = nn.Linear(self.hidden_size, self.intermediate_size, bias=False)
        self.down_proj = nn.Linear(self.intermediate_size, self.hidden_size, bias=False)
        self.act_fn = ACT2FN[config.hidden_act]

    def forward(self, x):
        down_proj = self.down_proj(self.act_fn(self.gate_proj(x)) * self.up_proj(x))
        return down_proj


class Qwen3VLTextDecoderLayer(GradientCheckpointingLayer):
    def __init__(self, config: Qwen3VLTextConfig, layer_idx: int):
        super().__init__()
        self.hidden_size = config.hidden_size

        self.self_attn = Qwen3VLTextAttention(config=config, layer_idx=layer_idx)

        self.mlp = Qwen3VLTextMLP(config)
        self.input_layernorm = Qwen3VLTextRMSNorm(config.hidden_size, eps=config.rms_norm_eps)
        self.post_attention_layernorm = Qwen3VLTextRMSNorm(config.hidden_size, eps=config.rms_norm_eps)

    @deprecate_kwarg("past_key_value", new_name="past_key_values", version="4.58")
    def forward(
        self,
        hidden_states: torch.Tensor,
        position_embeddings: tuple[torch.Tensor, torch.Tensor],
        attention_mask: Optional[torch.Tensor] = None,
        position_ids: Optional[torch.LongTensor] = None,
        past_key_values: Optional[Cache] = None,
        use_cache: Optional[bool] = False,
        cache_position: Optional[torch.LongTensor] = None,
        panoramic_mask: Optional[torch.Tensor] = None,
        panoramic_widths: Optional[torch.Tensor] = None,
        **kwargs: Unpack[TransformersKwargs],
    ) -> torch.Tensor:
        residual = hidden_states
        hidden_states = self.input_layernorm(hidden_states)
        # Self Attention
        hidden_states, _ = self.self_attn(
            hidden_states=hidden_states,
            attention_mask=attention_mask,
            past_key_values=past_key_values,
            use_cache=use_cache,
            cache_position=cache_position,
            position_embeddings=position_embeddings,
            panoramic_mask=panoramic_mask,
            panoramic_widths=panoramic_widths,
            position_ids=position_ids,
            **kwargs,
        )
        hidden_states = residual + hidden_states

        # Fully Connected
        residual = hidden_states
        hidden_states = self.post_attention_layernorm(hidden_states)
        hidden_states = self.mlp(hidden_states)
        hidden_states = residual + hidden_states
        return hidden_states


@dataclass
@auto_docstring(
    custom_intro="""
    Base class for Llava outputs, with hidden states and attentions.
    """
)
class Qwen3VLModelOutputWithPast(ModelOutput):
    r"""
    past_key_values (`Cache`, *optional*, returned when `use_cache=True` is passed or when `config.use_cache=True`):
        It is a [`~cache_utils.Cache`] instance. For more details, see our [kv cache guide](https://huggingface.co/docs/transformers/en/kv_cache).

        Contains pre-computed hidden-states (key and values in the self-attention blocks) that can be used (see
        `past_key_values` input) to speed up sequential decoding.
    rope_deltas (`torch.LongTensor` of shape `(batch_size, )`, *optional*):
        The rope index difference between sequence length and multimodal rope.
    """

    last_hidden_state: Optional[torch.FloatTensor] = None
    past_key_values: Optional[Cache] = None
    hidden_states: Optional[tuple[torch.FloatTensor]] = None
    attentions: Optional[tuple[torch.FloatTensor]] = None
    rope_deltas: Optional[torch.LongTensor] = None


@auto_docstring
class Qwen3VLPreTrainedModel(PreTrainedModel):
    config: Qwen3VLConfig
    base_model_prefix = "model"
    supports_gradient_checkpointing = True
    _no_split_modules = ["Qwen3VLTextDecoderLayer", "Qwen3VLVisionBlock"]
    _skip_keys_device_placement = "past_key_values"
    _supports_flash_attn = True
    _supports_sdpa = True

    _can_compile_fullgraph = True
    _supports_attention_backend = True
    _can_record_outputs = {
        "hidden_states": Qwen3VLTextDecoderLayer,
        "attentions": Qwen3VLTextAttention,
    }


class Qwen3VLVisionModel(Qwen3VLPreTrainedModel):
    config: Qwen3VLVisionConfig
    _no_split_modules = ["Qwen3VLVisionBlock"]

    def __init__(self, config, *inputs, **kwargs) -> None:
        super().__init__(config, *inputs, **kwargs)
        self.spatial_merge_size = config.spatial_merge_size
        self.patch_size = config.patch_size
        self.spatial_merge_unit = self.spatial_merge_size * self.spatial_merge_size

        self.patch_embed = Qwen3VLVisionPatchEmbed(
            config=config,
        )

        self.pos_embed = nn.Embedding(config.num_position_embeddings, config.hidden_size)
        self.num_grid_per_side = int(config.num_position_embeddings**0.5)

        head_dim = config.hidden_size // config.num_heads
        self.rotary_pos_emb = Qwen3VLVisionRotaryEmbedding(head_dim // 2)

        self.blocks = nn.ModuleList([Qwen3VLVisionBlock(config) for _ in range(config.depth)])
        self.merger = Qwen3VLVisionPatchMerger(
            config=config,
            use_postshuffle_norm=False,
        )

        self.deepstack_visual_indexes = config.deepstack_visual_indexes
        self.deepstack_merger_list = nn.ModuleList(
            [
                Qwen3VLVisionPatchMerger(
                    config=config,
                    use_postshuffle_norm=True,
                )
                for _ in range(len(config.deepstack_visual_indexes))
            ]
        )

        self.gradient_checkpointing = False

    def rot_pos_emb(self, grid_thw: torch.Tensor) -> torch.Tensor:
        merge_size = self.spatial_merge_size

        max_hw = int(grid_thw[:, 1:].max().item())
        freq_table = self.rotary_pos_emb(max_hw)  # (max_hw, dim // 2)
        device = freq_table.device

        total_tokens = int(torch.prod(grid_thw, dim=1).sum().item())
        pos_ids = torch.empty((total_tokens, 2), dtype=torch.long, device=device)

        offset = 0
        for num_frames, height, width in grid_thw:
            merged_h, merged_w = height // merge_size, width // merge_size

            block_rows = torch.arange(merged_h, device=device)  # block row indices
            block_cols = torch.arange(merged_w, device=device)  # block col indices
            intra_row = torch.arange(merge_size, device=device)  # intra-block row offsets
            intra_col = torch.arange(merge_size, device=device)  # intra-block col offsets

            # Compute full-resolution positions
            row_idx = block_rows[:, None, None, None] * merge_size + intra_row[None, None, :, None]
            col_idx = block_cols[None, :, None, None] * merge_size + intra_col[None, None, None, :]

            row_idx = row_idx.expand(merged_h, merged_w, merge_size, merge_size).reshape(-1)
            col_idx = col_idx.expand(merged_h, merged_w, merge_size, merge_size).reshape(-1)

            coords = torch.stack((row_idx, col_idx), dim=-1)

            if num_frames > 1:
                coords = coords.repeat(num_frames, 1)

            num_tokens = coords.shape[0]
            pos_ids[offset : offset + num_tokens] = coords
            offset += num_tokens

        embeddings = freq_table[pos_ids]  # lookup rotary embeddings
        embeddings = embeddings.flatten(1)
        return embeddings

    def fast_pos_embed_interpolate(self, grid_thw):
        grid_ts, grid_hs, grid_ws = grid_thw[:, 0], grid_thw[:, 1], grid_thw[:, 2]

        idx_list = [[] for _ in range(4)]
        weight_list = [[] for _ in range(4)]

        for t, h, w in zip(grid_ts, grid_hs, grid_ws):
            h_idxs = torch.linspace(0, self.num_grid_per_side - 1, h)
            w_idxs = torch.linspace(0, self.num_grid_per_side - 1, w)

            h_idxs_floor = h_idxs.int()
            w_idxs_floor = w_idxs.int()
            h_idxs_ceil = (h_idxs.int() + 1).clip(max=self.num_grid_per_side - 1)
            w_idxs_ceil = (w_idxs.int() + 1).clip(max=self.num_grid_per_side - 1)

            dh = h_idxs - h_idxs_floor
            dw = w_idxs - w_idxs_floor

            base_h = h_idxs_floor * self.num_grid_per_side
            base_h_ceil = h_idxs_ceil * self.num_grid_per_side

            indices = [
                (base_h[None].T + w_idxs_floor[None]).flatten(),
                (base_h[None].T + w_idxs_ceil[None]).flatten(),
                (base_h_ceil[None].T + w_idxs_floor[None]).flatten(),
                (base_h_ceil[None].T + w_idxs_ceil[None]).flatten(),
            ]

            weights = [
                ((1 - dh)[None].T * (1 - dw)[None]).flatten(),
                ((1 - dh)[None].T * dw[None]).flatten(),
                (dh[None].T * (1 - dw)[None]).flatten(),
                (dh[None].T * dw[None]).flatten(),
            ]

            for i in range(4):
                idx_list[i].extend(indices[i].tolist())
                weight_list[i].extend(weights[i].tolist())

        # Use grid_thw.device instead of self.pos_embed.weight.device for FSDP compatibility
        target_device = grid_thw.device if grid_thw.device.type != 'cpu' else self.pos_embed.weight.device
        idx_tensor = torch.tensor(idx_list, dtype=torch.long, device=target_device)
        weight_tensor = torch.tensor(
            weight_list, dtype=self.pos_embed.weight.dtype, device=target_device
        )
        pos_embeds = self.pos_embed(idx_tensor) * weight_tensor[:, :, None]
        patch_pos_embeds = pos_embeds[0] + pos_embeds[1] + pos_embeds[2] + pos_embeds[3]

        patch_pos_embeds = patch_pos_embeds.split([h * w for h, w in zip(grid_hs, grid_ws)])

        patch_pos_embeds_permute = []
        merge_size = self.config.spatial_merge_size
        for pos_embed, t, h, w in zip(patch_pos_embeds, grid_ts, grid_hs, grid_ws):
            pos_embed = pos_embed.repeat(t, 1)
            pos_embed = (
                pos_embed.view(t, h // merge_size, merge_size, w // merge_size, merge_size, -1)
                .permute(0, 1, 3, 2, 4, 5)
                .flatten(0, 4)
            )
            patch_pos_embeds_permute.append(pos_embed)
        patch_pos_embeds = torch.cat(patch_pos_embeds_permute)
        return patch_pos_embeds

    def forward(self, hidden_states: torch.Tensor, grid_thw: torch.Tensor, **kwargs) -> torch.Tensor:
        """
        Args:
            hidden_states (`torch.Tensor` of shape `(seq_len, hidden_size)`):
                The final hidden states of the model.
            grid_thw (`torch.Tensor` of shape `(num_images_or_videos, 3)`):
                The temporal, height and width of feature shape of each image in LLM.

        Returns:
            `torch.Tensor`: hidden_states.
        """
        hidden_states = self.patch_embed(hidden_states)

        pos_embeds = self.fast_pos_embed_interpolate(grid_thw)
        hidden_states = hidden_states + pos_embeds

        rotary_pos_emb = self.rot_pos_emb(grid_thw)

        seq_len, _ = hidden_states.size()
        hidden_states = hidden_states.reshape(seq_len, -1)
        rotary_pos_emb = rotary_pos_emb.reshape(seq_len, -1)
        emb = torch.cat((rotary_pos_emb, rotary_pos_emb), dim=-1)
        position_embeddings = (emb.cos(), emb.sin())

        cu_seqlens = torch.repeat_interleave(grid_thw[:, 1] * grid_thw[:, 2], grid_thw[:, 0]).cumsum(
            dim=0,
            # Select dtype based on the following factors:
            #  - FA2 requires that cu_seqlens_q must have dtype int32
            #  - torch.onnx.export requires that cu_seqlens_q must have same dtype as grid_thw
            # See https://github.com/huggingface/transformers/pull/34852 for more information
            dtype=grid_thw.dtype if torch.jit.is_tracing() else torch.int32,
        )
        cu_seqlens = F.pad(cu_seqlens, (1, 0), value=0)

        deepstack_feature_lists = []
        for layer_num, blk in enumerate(self.blocks):
            hidden_states = blk(
                hidden_states,
                cu_seqlens=cu_seqlens,
                position_embeddings=position_embeddings,
                **kwargs,
            )
            if layer_num in self.deepstack_visual_indexes:
                deepstack_feature = self.deepstack_merger_list[self.deepstack_visual_indexes.index(layer_num)](
                    hidden_states
                )
                deepstack_feature_lists.append(deepstack_feature)

        hidden_states = self.merger(hidden_states)

        return hidden_states, deepstack_feature_lists


@auto_docstring(
    custom_intro=(
        "Text part of Qwen3VL, "
        "not a pure text-only model, as DeepStack integrates visual features into the early hidden states."
    )
)
class Qwen3VLTextModel(Qwen3VLPreTrainedModel):
    config: Qwen3VLTextConfig
    _no_split_modules = ["Qwen3VLTextDecoderLayer"]

    def __init__(self, config: Qwen3VLTextConfig):
        super().__init__(config)
        self.padding_idx = config.pad_token_id
        self.vocab_size = config.vocab_size

        self.embed_tokens = nn.Embedding(config.vocab_size, config.hidden_size, self.padding_idx)
        self.layers = nn.ModuleList(
            [Qwen3VLTextDecoderLayer(config, layer_idx) for layer_idx in range(config.num_hidden_layers)]
        )
        self.norm = Qwen3VLTextRMSNorm(config.hidden_size, eps=config.rms_norm_eps)
        self.rotary_emb = Qwen3VLTextRotaryEmbedding(config=config)
        self.gradient_checkpointing = False

        # Initialize weights and apply final processing
        self.post_init()

    @check_model_inputs
    @auto_docstring
    def forward(
        self,
        input_ids: Optional[torch.LongTensor] = None,
        attention_mask: Optional[torch.Tensor] = None,
        position_ids: Optional[torch.LongTensor] = None,
        past_key_values: Optional[Cache] = None,
        inputs_embeds: Optional[torch.FloatTensor] = None,
        use_cache: Optional[bool] = None,
        cache_position: Optional[torch.LongTensor] = None,
        # args for deepstack
        visual_pos_masks: Optional[torch.Tensor] = None,
        deepstack_visual_embeds: Optional[list[torch.Tensor]] = None,
        # args for panoramic multi-head rolling
        panoramic_mask: Optional[torch.Tensor] = None,
        panoramic_widths: Optional[torch.Tensor] = None,
        **kwargs: Unpack[FlashAttentionKwargs],
    ) -> Union[tuple, BaseModelOutputWithPast]:
        r"""
        visual_pos_masks (`torch.Tensor` of shape `(batch_size, seqlen)`, *optional*):
            The mask of the visual positions.
        deepstack_visual_embeds (`list[torch.Tensor]`, *optional*):
            The deepstack visual embeddings. The shape is (num_layers, visual_seqlen, embed_dim).
            The feature is extracted from the different visual encoder layers, and fed to the decoder
            hidden states. It's from the paper DeepStack(https://arxiv.org/abs/2406.04334).
        """
        if (input_ids is None) ^ (inputs_embeds is not None):
            raise ValueError("You must specify exactly one of input_ids or inputs_embeds")

        # torch.jit.trace() doesn't support cache objects in the output
        if use_cache and past_key_values is None and not torch.jit.is_tracing():
            past_key_values = DynamicCache(config=self.config)

        if inputs_embeds is None:
            inputs_embeds = self.embed_tokens(input_ids)

        if cache_position is None:
            past_seen_tokens = past_key_values.get_seq_length() if past_key_values is not None else 0
            cache_position = torch.arange(
                past_seen_tokens, past_seen_tokens + inputs_embeds.shape[1], device=inputs_embeds.device
            )

        # the hard coded `3` is for temporal, height and width.
        if position_ids is None:
            position_ids = cache_position.view(1, 1, -1).expand(3, inputs_embeds.shape[0], -1)
        elif position_ids.ndim == 2:
            position_ids = position_ids[None, ...].expand(3, position_ids.shape[0], -1)

        if position_ids.ndim == 3 and position_ids.shape[0] == 4:
            text_position_ids = position_ids[0]
            position_ids = position_ids[1:]
        else:
            text_position_ids = position_ids[0]

        attention_mask = create_causal_mask(
            config=self.config,
            input_embeds=inputs_embeds,
            attention_mask=attention_mask,
            cache_position=cache_position,
            past_key_values=past_key_values,
            position_ids=text_position_ids,
        )

        hidden_states = inputs_embeds

        # create position embeddings to be shared across the decoder layers
        position_embeddings = self.rotary_emb(hidden_states, position_ids)

        # decoder layers
        for layer_idx, decoder_layer in enumerate(self.layers):
            layer_outputs = decoder_layer(
                hidden_states,
                attention_mask=attention_mask,
                position_ids=text_position_ids,
                past_key_values=past_key_values,
                cache_position=cache_position,
                position_embeddings=position_embeddings,
                panoramic_mask=panoramic_mask,
                panoramic_widths=panoramic_widths,
                **kwargs,
            )
            hidden_states = layer_outputs

            # add visual features to the hidden states of first several layers
            if deepstack_visual_embeds is not None and layer_idx in range(len(deepstack_visual_embeds)):
                hidden_states = self._deepstack_process(
                    hidden_states,
                    visual_pos_masks,
                    deepstack_visual_embeds[layer_idx],
                )

        hidden_states = self.norm(hidden_states)

        return BaseModelOutputWithPast(
            last_hidden_state=hidden_states,
            past_key_values=past_key_values,
        )

    def _deepstack_process(
        self, hidden_states: torch.Tensor, visual_pos_masks: torch.Tensor, visual_embeds: torch.Tensor
    ):
        visual_pos_masks = visual_pos_masks.to(hidden_states.device)
        visual_embeds = visual_embeds.to(hidden_states.device, hidden_states.dtype)
        local_this = hidden_states[visual_pos_masks, :].clone() + visual_embeds
        hidden_states[visual_pos_masks, :] = local_this
        return hidden_states


@auto_docstring
class Qwen3VLModel(Qwen3VLPreTrainedModel):
    base_model_prefix = ""
    _checkpoint_conversion_mapping = {}
    # Reference: fix gemma3 grad acc #37208
    accepts_loss_kwargs = False
    config: Qwen3VLConfig
    _no_split_modules = ["Qwen3VLTextDecoderLayer", "Qwen3VLVisionBlock"]

    def __init__(self, config):
        super().__init__(config)
        self.visual = Qwen3VLVisionModel._from_config(config.vision_config)
        self.language_model = Qwen3VLTextModel._from_config(config.text_config)
        self.rope_deltas = None  # cache rope_deltas here
        self.panoramic_pos_mask = None  # cache panoramic position mask
        self.panoramic_width_map = None  # cache panoramic width map

        # Initialize weights and apply final processing
        self.post_init()

    def get_input_embeddings(self):
        return self.language_model.get_input_embeddings()

    def set_input_embeddings(self, value):
        self.language_model.set_input_embeddings(value)

    def set_decoder(self, decoder):
        self.language_model = decoder

    def get_decoder(self):
        return self.language_model

    def get_rope_index(
        self,
        input_ids: Optional[torch.LongTensor] = None,
        image_grid_thw: Optional[torch.LongTensor] = None,
        video_grid_thw: Optional[torch.LongTensor] = None,
        panoramic_grid_thw: Optional[torch.LongTensor] = None,
        attention_mask: Optional[torch.Tensor] = None,
    ) -> tuple[torch.Tensor, torch.Tensor, Optional[torch.Tensor], Optional[torch.Tensor]]:
        """Different from the original implementation, Qwen3VL use timestamps rather than absolute time position ids."""
        import math
        
        # Since we use timestamps to seperate videos, like <t1> <vision_start> <frame1> <vision_end> <t2> <vision_start> <frame2> <vision_end>, the video_grid_thw should also be split
        if video_grid_thw is not None:
            video_grid_thw = torch.repeat_interleave(video_grid_thw, video_grid_thw[:, 0], dim=0)
            video_grid_thw[:, 0] = 1

        spatial_merge_size = self.config.vision_config.spatial_merge_size
        image_token_id = self.config.image_token_id
        video_token_id = self.config.video_token_id
        panoramic_image_token_id = self.config.panoramic_image_token_id
        vision_start_token_id = self.config.vision_start_token_id
        mrope_position_deltas = []
        if input_ids is not None and (image_grid_thw is not None or video_grid_thw is not None or panoramic_grid_thw is not None):
            total_input_ids = input_ids
            if attention_mask is None:
                attention_mask = torch.ones_like(total_input_ids)
            # 使用float32类型以支持全景图像的浮点位置编码
            position_ids = torch.ones(
                3,
                input_ids.shape[0],
                input_ids.shape[1],
                dtype=torch.float32,
                device=input_ids.device,
            )
            # 创建全景图位置mask和width信息
            panoramic_pos_mask = torch.zeros(
                input_ids.shape[0],
                input_ids.shape[1],
                dtype=torch.bool,
                device=input_ids.device,
            )
            panoramic_width_map = torch.zeros(
                input_ids.shape[0],
                input_ids.shape[1],
                dtype=torch.long,
                device=input_ids.device,
            )
            image_index, video_index, panoramic_index = 0, 0, 0
            attention_mask = attention_mask.to(total_input_ids.device)
            for i, input_ids in enumerate(total_input_ids):
                input_ids = input_ids[attention_mask[i] == 1]
                image_nums, video_nums, panoramic_nums = 0, 0, 0
                vision_start_indices = torch.argwhere(input_ids == vision_start_token_id).squeeze(1)
                vision_tokens = input_ids[vision_start_indices + 1]
                image_nums = (vision_tokens == image_token_id).sum()
                video_nums = (vision_tokens == video_token_id).sum()
                panoramic_nums = (vision_tokens == panoramic_image_token_id).sum()
                input_tokens = input_ids.tolist()
                llm_pos_ids_list: list = []
                st = 0
                remain_images, remain_videos, remain_panoramic = image_nums, video_nums, panoramic_nums
                for _ in range(image_nums + video_nums + panoramic_nums):
                    if image_token_id in input_tokens and remain_images > 0:
                        ed_image = input_tokens.index(image_token_id, st)
                    else:
                        ed_image = len(input_tokens) + 1
                    if video_token_id in input_tokens and remain_videos > 0:
                        ed_video = input_tokens.index(video_token_id, st)
                    else:
                        ed_video = len(input_tokens) + 1
                    if panoramic_image_token_id in input_tokens and remain_panoramic > 0:
                        ed_panoramic = input_tokens.index(panoramic_image_token_id, st)
                    else:
                        ed_panoramic = len(input_tokens) + 1
                    
                    # Find which token comes first and set is_panoramic flag
                    min_ed = min(ed_image, ed_video, ed_panoramic)
                    if min_ed == ed_image:
                        t, h, w = (
                            image_grid_thw[image_index][0],
                            image_grid_thw[image_index][1],
                            image_grid_thw[image_index][2],
                        )
                        image_index += 1
                        remain_images -= 1
                        ed = ed_image
                        is_panoramic = False
                    elif min_ed == ed_panoramic:
                        t, h, w = (
                            panoramic_grid_thw[panoramic_index][0],
                            panoramic_grid_thw[panoramic_index][1],
                            panoramic_grid_thw[panoramic_index][2],
                        )
                        panoramic_index += 1
                        remain_panoramic -= 1
                        ed = ed_panoramic
                        is_panoramic = True
                    else:
                        t, h, w = (
                            video_grid_thw[video_index][0],
                            video_grid_thw[video_index][1],
                            video_grid_thw[video_index][2],
                        )
                        video_index += 1
                        remain_videos -= 1
                        ed = ed_video
                        is_panoramic = False
                        
                    llm_grid_t, llm_grid_h, llm_grid_w = (
                        t.item(),
                        h.item() // spatial_merge_size,
                        w.item() // spatial_merge_size,
                    )
                    text_len = ed - st

                    st_idx = llm_pos_ids_list[-1].max() + 1 if len(llm_pos_ids_list) > 0 else 0
                    llm_pos_ids_list.append(torch.arange(text_len, dtype=torch.float32).view(1, -1).expand(3, -1) + st_idx)
                    
                    # 记录全景图token的位置和width
                    if is_panoramic:
                        # 找到这批全景图tokens在原始序列中的位置
                        valid_mask = attention_mask[i] == 1
                        valid_positions = torch.where(valid_mask)[0]
                        vision_token_start = ed
                        vision_token_end = ed + llm_grid_t * llm_grid_h * llm_grid_w
                        if vision_token_start < len(valid_positions) and vision_token_end <= len(valid_positions):
                            original_start = valid_positions[vision_token_start]
                            original_end = valid_positions[vision_token_end - 1] + 1
                            panoramic_pos_mask[i, original_start:original_end] = True
                            panoramic_width_map[i, original_start:original_end] = llm_grid_w

                    if is_panoramic:
                        # 为全景图像生成特殊的位置索引，使用浮点数类型保持精度
                        # t_index = torch.arange(llm_grid_t, dtype=torch.float32).view(-1, 1).expand(-1, llm_grid_h * llm_grid_w).flatten()
                        # # DENSE 360!!!!!!!!
                        # h_index = (
                        #     torch.arange(1, llm_grid_h + 1, dtype=torch.float32)
                        #     .view(1, -1, 1)
                        #     .expand(llm_grid_t, -1, llm_grid_w)
                        #     .flatten()
                        # )
                        
                        # 根据 w 是奇数还是偶数，生成不同的映射
                        # if llm_grid_w % 2 == 1:  # 奇数
                        #     # f(w) := [1, 2, 3, ..., (W+1)/2, (W+1)/2, (W+1)/2-1, ..., 3, 2]
                        #     mid = (llm_grid_w + 1) // 2
                        #     first_half = list(range(1, mid + 1))  # [1, 2, ..., mid]
                        #     second_half = list(range(mid, 1, -1))  # [mid, mid-1, ..., 2]
                        #     w_vals = torch.tensor(first_half + second_half, dtype=torch.float32)
                        # else:  # 偶数
                        #     # f(w) := [1, 2, 3, ..., W/2+1, W/2, ..., 3, 2]
                        #     max_val = llm_grid_w // 2 + 1
                        #     first_half = list(range(1, max_val + 1))  # [1, 2, ..., W/2+1]
                        #     second_half = list(range(llm_grid_w // 2, 1, -1))  # [W/2, W/2-1, ..., 2]
                        #     w_vals = torch.tensor(first_half + second_half, dtype=torch.float32)
                        # w_vals = torch.arange(1, llm_grid_w + 1, dtype=torch.float32)
                        # # 计算每一行的 cos 系数: cos(π/H * (h - (H+1)/2))
                        # h_vals = torch.arange(1, llm_grid_h + 1, dtype=torch.float32)  # [1, 2, 3, ..., H]
                        # cos_coeffs = torch.cos(math.pi / llm_grid_h * (h_vals - (llm_grid_h + 1) / 2))  # shape: [H]
                        # sum_cos_coeffs = cos_coeffs.sum()
                        # cos_coeffs = cos_coeffs * llm_grid_h / sum_cos_coeffs
                        # # 将每一行的 w_vals 乘以对应的 cos 系数
                        # # cos_coeffs: [H], w_vals: [W] -> [H, W]
                        # w_vals_scaled = cos_coeffs.unsqueeze(1) * w_vals.unsqueeze(0)  # shape: [H, W]
                        
                        # # 再 expand 到 t 维度
                        # w_index = (
                        #     w_vals_scaled.unsqueeze(0)  # [1, H, W]
                        #     .expand(llm_grid_t, -1, -1)  # [T, H, W]
                        #     .flatten()
                        # )
                        t_index = torch.arange(llm_grid_t, dtype=torch.float32).view(-1, 1).expand(-1, llm_grid_h * llm_grid_w).flatten()
                        h_index = torch.arange(llm_grid_h, dtype=torch.float32).view(1, -1, 1).expand(llm_grid_t, -1, llm_grid_w).flatten()
                        w_index = torch.arange(llm_grid_w, dtype=torch.float32).view(1, 1, -1).expand(llm_grid_t, llm_grid_h, -1).flatten()
                    else:
                        # t_index is always 0 because llm_grid_t is always 1 (we use timestamps to encode the temporal information for videos)
                        # 统一使用float32类型，保持与panoramic一致
                        t_index = torch.arange(llm_grid_t, dtype=torch.float32).view(-1, 1).expand(-1, llm_grid_h * llm_grid_w).flatten()
                        h_index = torch.arange(llm_grid_h, dtype=torch.float32).view(1, -1, 1).expand(llm_grid_t, -1, llm_grid_w).flatten()
                        w_index = torch.arange(llm_grid_w, dtype=torch.float32).view(1, 1, -1).expand(llm_grid_t, llm_grid_h, -1).flatten()
                    
                    # 所有类型统一为float32，直接stack即可
                    llm_pos_ids_list.append(torch.stack([t_index, h_index, w_index]) + text_len + st_idx)
                    st = ed + llm_grid_t * llm_grid_h * llm_grid_w

                if st < len(input_tokens):
                    st_idx = llm_pos_ids_list[-1].max() + 1 if len(llm_pos_ids_list) > 0 else 0
                    text_len = len(input_tokens) - st
                    llm_pos_ids_list.append(torch.arange(text_len, dtype=torch.float32).view(1, -1).expand(3, -1) + st_idx)

                llm_positions = torch.cat(llm_pos_ids_list, dim=1).reshape(3, -1)
                position_ids[..., i, attention_mask[i] == 1] = llm_positions.to(position_ids.device)
                mrope_position_deltas.append(llm_positions.max() + 1 - len(total_input_ids[i]))
            mrope_position_deltas = torch.tensor(mrope_position_deltas, dtype=torch.float32, device=input_ids.device).unsqueeze(1)
            return position_ids, mrope_position_deltas, panoramic_pos_mask, panoramic_width_map
        else:
            if attention_mask is not None:
                position_ids = attention_mask.long().cumsum(-1) - 1
                position_ids.masked_fill_(attention_mask == 0, 1)
                position_ids = position_ids.unsqueeze(0).expand(3, -1, -1).to(attention_mask.device)
                max_position_ids = position_ids.max(0, keepdim=False)[0].max(-1, keepdim=True)[0]
                mrope_position_deltas = max_position_ids + 1 - attention_mask.shape[-1]
            else:
                position_ids = (
                    torch.arange(input_ids.shape[1], dtype=torch.float32, device=input_ids.device)
                    .view(1, 1, -1)
                    .expand(3, input_ids.shape[0], -1)
                )
                mrope_position_deltas = torch.zeros(
                    [input_ids.shape[0], 1],
                    device=input_ids.device,
                    dtype=torch.float32,
                )

            return position_ids, mrope_position_deltas, None, None

    def get_video_features(
        self, pixel_values_videos: torch.FloatTensor, video_grid_thw: Optional[torch.LongTensor] = None
    ):
        """
        Encodes videos into continuous embeddings that can be forwarded to the language model. The deepstack visual features are also returned.

        Args:
            pixel_values_videos (`torch.FloatTensor` of shape `(batch_size, num_channels, image_size, image_size)`):
                The tensors corresponding to the input videos.
            video_grid_thw (`torch.LongTensor` of shape `(num_videos, 3)`, *optional*):
                The temporal, height and width of feature shape of each video in LLM.
        """
        # Same implementation as for images
        return self.get_image_features(pixel_values_videos, video_grid_thw)

    def get_panoramic_image_features(
        self, pixel_values_panoramic: torch.FloatTensor, panoramic_grid_thw: Optional[torch.LongTensor] = None
    ):
        """
        Encodes panoramic images into continuous embeddings that can be forwarded to the language model. The deepstack visual features are also returned.

        Args:
            pixel_values_panoramic (`torch.FloatTensor` of shape `(batch_size, num_channels, image_size, image_size)`):
                The tensors corresponding to the input panoramic images.
            panoramic_grid_thw (`torch.LongTensor` of shape `(num_panoramic_images, 3)`, *optional*):
                The temporal, height and width of feature shape of each panoramic image in LLM.
        """
        # Same implementation as for images
        return self.get_image_features(pixel_values_panoramic, panoramic_grid_thw)

    def get_image_features(self, pixel_values: torch.FloatTensor, image_grid_thw: Optional[torch.LongTensor] = None):
        """
        Encodes images into continuous embeddings that can be forwarded to the language model. The deepstack visual features are also returned.

        Args:
            pixel_values (`torch.FloatTensor` of shape `(batch_size, num_channels, image_size, image_size)`):
                The tensors corresponding to the input images.
            image_grid_thw (`torch.LongTensor` of shape `(num_images, 3)`, *optional*):
                The temporal, height and width of feature shape of each image in LLM.
        """
        pixel_values = pixel_values.type(self.visual.dtype)
        image_embeds, deepstack_image_embeds = self.visual(pixel_values, grid_thw=image_grid_thw)
        split_sizes = (image_grid_thw.prod(-1) // self.visual.spatial_merge_size**2).tolist()
        print(f"[DEBUG get_image_features] image_grid_thw={image_grid_thw.tolist()}, split_sizes={split_sizes}, total_features={sum(split_sizes)}, image_embeds.shape={image_embeds.shape}", flush=True)
        image_embeds = torch.split(image_embeds, split_sizes)
        return image_embeds, deepstack_image_embeds

    def get_placeholder_mask(
        self,
        input_ids: torch.LongTensor,
        inputs_embeds: torch.FloatTensor,
        image_features: Optional[torch.FloatTensor] = None,
        video_features: Optional[torch.FloatTensor] = None,
        panoramic_features: Optional[torch.FloatTensor] = None,
    ):
        """
        Obtains multimodal placeholder mask from `input_ids` or `inputs_embeds`, and checks that the placeholder token count is
        equal to the length of multimodal features. If the lengths are different, an error is raised.
        """
        if input_ids is None:
            special_image_mask = inputs_embeds == self.get_input_embeddings()(
                torch.tensor(self.config.image_token_id, dtype=torch.long, device=inputs_embeds.device)
            )
            special_image_mask = special_image_mask.all(-1)
            special_video_mask = inputs_embeds == self.get_input_embeddings()(
                torch.tensor(self.config.video_token_id, dtype=torch.long, device=inputs_embeds.device)
            )
            special_video_mask = special_video_mask.all(-1)
            special_panoramic_mask = inputs_embeds == self.get_input_embeddings()(
                torch.tensor(self.config.panoramic_image_token_id, dtype=torch.long, device=inputs_embeds.device)
            )
            special_panoramic_mask = special_panoramic_mask.all(-1)
        else:
            special_image_mask = input_ids == self.config.image_token_id
            special_video_mask = input_ids == self.config.video_token_id
            special_panoramic_mask = input_ids == self.config.panoramic_image_token_id

        n_image_tokens = special_image_mask.sum()
        special_image_mask = special_image_mask.unsqueeze(-1).expand_as(inputs_embeds).to(inputs_embeds.device)
        if image_features is not None and inputs_embeds[special_image_mask].numel() != image_features.numel():
            # Debug info
            embed_dim = inputs_embeds.shape[-1]
            expected_numel = n_image_tokens * embed_dim
            actual_numel = image_features.numel()
            print(f"[DEBUG MISMATCH] n_image_tokens={n_image_tokens}, image_features.shape={image_features.shape}", flush=True)
            print(f"[DEBUG MISMATCH] input_ids unique tokens with image_token_id: {(input_ids == self.config.image_token_id).sum()}", flush=True)
            raise ValueError(
                f"Image features and image tokens do not match: tokens: {n_image_tokens}, features {image_features.shape[0]}"
            )

        n_video_tokens = special_video_mask.sum()
        special_video_mask = special_video_mask.unsqueeze(-1).expand_as(inputs_embeds).to(inputs_embeds.device)
        if video_features is not None and inputs_embeds[special_video_mask].numel() != video_features.numel():
            raise ValueError(
                f"Videos features and video tokens do not match: tokens: {n_video_tokens}, features {video_features.shape[0]}"
            )

        n_panoramic_tokens = special_panoramic_mask.sum()
        special_panoramic_mask = special_panoramic_mask.unsqueeze(-1).expand_as(inputs_embeds).to(inputs_embeds.device)
        if panoramic_features is not None and inputs_embeds[special_panoramic_mask].numel() != panoramic_features.numel():
            raise ValueError(
                f"Panoramic features and panoramic tokens do not match: tokens: {n_panoramic_tokens}, features {panoramic_features.shape[0]}"
            )

        return special_image_mask, special_video_mask, special_panoramic_mask

    @auto_docstring
    @check_model_inputs
    def forward(
        self,
        input_ids: torch.LongTensor = None,
        attention_mask: Optional[torch.Tensor] = None,
        position_ids: Optional[torch.LongTensor] = None,
        past_key_values: Optional[Cache] = None,
        inputs_embeds: Optional[torch.FloatTensor] = None,
        pixel_values: Optional[torch.Tensor] = None,
        pixel_values_videos: Optional[torch.FloatTensor] = None,
        pixel_values_panoramic: Optional[torch.FloatTensor] = None,
        image_grid_thw: Optional[torch.LongTensor] = None,
        video_grid_thw: Optional[torch.LongTensor] = None,
        panoramic_grid_thw: Optional[torch.LongTensor] = None,
        cache_position: Optional[torch.LongTensor] = None,
        panoramic_pos_mask: Optional[torch.Tensor] = None,
        panoramic_width_map: Optional[torch.Tensor] = None,
        **kwargs: Unpack[TransformersKwargs],
    ) -> Union[tuple, Qwen3VLModelOutputWithPast]:
        r"""
        image_grid_thw (`torch.LongTensor` of shape `(num_images, 3)`, *optional*):
            The temporal, height and width of feature shape of each image in LLM.
        video_grid_thw (`torch.LongTensor` of shape `(num_videos, 3)`, *optional*):
            The temporal, height and width of feature shape of each video in LLM.
        panoramic_grid_thw (`torch.LongTensor` of shape `(num_panoramic_images, 3)`, *optional*):
            The temporal, height and width of feature shape of each panoramic image in LLM.
        """
        if (input_ids is None) ^ (inputs_embeds is not None):
            raise ValueError("You must specify exactly one of input_ids or inputs_embeds")

        if inputs_embeds is None:
            inputs_embeds = self.get_input_embeddings()(input_ids)

        image_mask = None
        video_mask = None
        panoramic_mask = None

        if pixel_values is not None:
            image_embeds, deepstack_image_embeds = self.get_image_features(pixel_values, image_grid_thw)
            image_embeds = torch.cat(image_embeds, dim=0).to(inputs_embeds.device, inputs_embeds.dtype)
            image_mask, _, _ = self.get_placeholder_mask(
                input_ids, inputs_embeds=inputs_embeds, image_features=image_embeds
            )
            inputs_embeds = inputs_embeds.masked_scatter(image_mask, image_embeds)

        if pixel_values_videos is not None:
            video_embeds, deepstack_video_embeds = self.get_video_features(pixel_values_videos, video_grid_thw)
            video_embeds = torch.cat(video_embeds, dim=0).to(inputs_embeds.device, inputs_embeds.dtype)
            _, video_mask, _ = self.get_placeholder_mask(
                input_ids, inputs_embeds=inputs_embeds, video_features=video_embeds
            )
            inputs_embeds = inputs_embeds.masked_scatter(video_mask, video_embeds)

        if pixel_values_panoramic is not None:
            panoramic_embeds, deepstack_panoramic_embeds = self.get_panoramic_image_features(pixel_values_panoramic, panoramic_grid_thw)
            panoramic_embeds = torch.cat(panoramic_embeds, dim=0).to(inputs_embeds.device, inputs_embeds.dtype)
            _, _, panoramic_mask = self.get_placeholder_mask(
                input_ids, inputs_embeds=inputs_embeds, panoramic_features=panoramic_embeds
            )
            inputs_embeds = inputs_embeds.masked_scatter(panoramic_mask, panoramic_embeds)

        visual_pos_masks = None
        deepstack_visual_embeds = None
        # Collect all masks and deepstack embeds
        all_masks = []
        all_deepstack_embeds = []
        if image_mask is not None:
            all_masks.append(("image", image_mask[..., 0], deepstack_image_embeds))
        if video_mask is not None:
            all_masks.append(("video", video_mask[..., 0], deepstack_video_embeds))
        if panoramic_mask is not None:
            all_masks.append(("panoramic", panoramic_mask[..., 0], deepstack_panoramic_embeds))
        
        if len(all_masks) > 1:
            # Multiple modalities present
            masks_only = [m for _, m, _ in all_masks]
            visual_pos_masks = masks_only[0]
            for mask in masks_only[1:]:
                visual_pos_masks = visual_pos_masks | mask
            
            deepstack_visual_embeds = []
            # Create joint masks for each modality
            joint_masks = {name: mask[visual_pos_masks] for name, mask, _ in all_masks}
            
            # Aggregate embeddings
            num_layers = len(all_masks[0][2])
            for layer_idx in range(num_layers):
                embed_joint = all_masks[0][2][layer_idx].new_zeros(
                    visual_pos_masks.sum(), all_masks[0][2][layer_idx].shape[-1]
                ).to(all_masks[0][2][layer_idx].device)
                for name, _, embeds in all_masks:
                    embed_joint[joint_masks[name], :] = embeds[layer_idx]
                deepstack_visual_embeds.append(embed_joint)
        elif len(all_masks) == 1:
            _, visual_pos_masks, deepstack_visual_embeds = all_masks[0]

        if position_ids is None:
            attention_mask_tensor = (
                attention_mask if not isinstance(attention_mask, dict) else attention_mask["full_attention"]
            )
            if attention_mask_tensor is not None and attention_mask_tensor.ndim == 4:
                attention_mask_tensor = torch.diagonal(attention_mask_tensor[:, 0], dim1=1, dim2=2)
                # Only apply conversion for floating point tensors (inverted masks)
                if attention_mask_tensor.dtype.is_floating_point:
                    attention_mask_tensor = attention_mask_tensor / torch.finfo(attention_mask_tensor.dtype).min
                    attention_mask_tensor = (1.0 - attention_mask_tensor).int()

            # Calculate RoPE index once per generation in the pre-fill stage only.
            # When compiling, we can't check tensor values thus we check only input length
            # It is safe to assume that `length!=1` means we're in pre-fill because compiled
            # models currently cannot do asssisted decoding
            prefill_compiled_stage = is_torchdynamo_compiling() and (
                (input_ids is not None and input_ids.shape[1] != 1)
                or (inputs_embeds is not None and inputs_embeds.shape[1] != 1)
            )
            prefill_noncompiled_stage = not is_torchdynamo_compiling() and (
                (cache_position is not None and cache_position[0] == 0)
                or (past_key_values is None or past_key_values.get_seq_length() == 0)
            )
            if (prefill_compiled_stage or prefill_noncompiled_stage) or self.rope_deltas is None:
                position_ids, rope_deltas, panoramic_pos_mask, panoramic_width_map = self.get_rope_index(
                    input_ids,
                    image_grid_thw,
                    video_grid_thw,
                    panoramic_grid_thw,
                    attention_mask=attention_mask_tensor,
                )
                self.rope_deltas = rope_deltas
                self.panoramic_pos_mask = panoramic_pos_mask
                self.panoramic_width_map = panoramic_width_map
            # then use the prev pre-calculated rope-deltas to get the correct position ids
            else:
                batch_size, seq_length, _ = inputs_embeds.shape
                delta = (
                    (cache_position[0] + self.rope_deltas).to(inputs_embeds.device)
                    if cache_position is not None
                    else 0
                )
                position_ids = torch.arange(seq_length, device=inputs_embeds.device)
                position_ids = position_ids.view(1, -1).expand(batch_size, -1)
                if cache_position is not None:  # otherwise `deltas` is an int `0`
                    delta = delta.repeat_interleave(batch_size // delta.shape[0], dim=0)
                position_ids = position_ids.add(delta)
                position_ids = position_ids.unsqueeze(0).expand(3, -1, -1)

        # 优先使用传入的全景图信息（训练时从DataCollator传入），否则使用缓存的（推理时从get_rope_index生成）
        final_panoramic_mask = panoramic_pos_mask if panoramic_pos_mask is not None else (
            self.panoramic_pos_mask if hasattr(self, 'panoramic_pos_mask') else None
        )
        final_panoramic_widths = panoramic_width_map if panoramic_width_map is not None else (
            self.panoramic_width_map if hasattr(self, 'panoramic_width_map') else None
        )
        
        outputs = self.language_model(
            input_ids=None,
            position_ids=position_ids,
            attention_mask=attention_mask,
            past_key_values=past_key_values,
            inputs_embeds=inputs_embeds,
            cache_position=cache_position,
            visual_pos_masks=visual_pos_masks,
            deepstack_visual_embeds=deepstack_visual_embeds,
            panoramic_mask=final_panoramic_mask,
            panoramic_widths=final_panoramic_widths,
            **kwargs,
        )

        return Qwen3VLModelOutputWithPast(
            last_hidden_state=outputs.last_hidden_state,
            past_key_values=outputs.past_key_values,
            rope_deltas=self.rope_deltas,
        )


@dataclass
@auto_docstring(
    custom_intro="""
    Base class for Qwen3VL causal language model (or autoregressive) outputs.
    """
)
class Qwen3VLCausalLMOutputWithPast(ModelOutput):
    r"""
    loss (`torch.FloatTensor` of shape `(1,)`, *optional*, returned when `labels` is provided):
        Language modeling loss (for next-token prediction).
    logits (`torch.FloatTensor` of shape `(batch_size, sequence_length, config.vocab_size)`):
        Prediction scores of the language modeling head (scores for each vocabulary token before SoftMax).
    past_key_values (`Cache`, *optional*, returned when `use_cache=True` is passed or when `config.use_cache=True`):
        It is a [`~cache_utils.Cache`] instance. For more details, see our [kv cache guide](https://huggingface.co/docs/transformers/en/kv_cache).

        Contains pre-computed hidden-states (key and values in the self-attention blocks) that can be used (see
        `past_key_values` input) to speed up sequential decoding.
    rope_deltas (`torch.LongTensor` of shape `(batch_size, )`, *optional*):
        The rope index difference between sequence length and multimodal rope.
    """

    loss: Optional[torch.FloatTensor] = None
    logits: Optional[torch.FloatTensor] = None
    past_key_values: Optional[Cache] = None
    hidden_states: Optional[tuple[torch.FloatTensor]] = None
    attentions: Optional[tuple[torch.FloatTensor]] = None
    rope_deltas: Optional[torch.LongTensor] = None


class Qwen3VLForConditionalGeneration(Qwen3VLPreTrainedModel, GenerationMixin):
    _checkpoint_conversion_mapping = {}
    _tied_weights_keys = ["lm_head.weight"]
    # Reference: fix gemma3 grad acc #37208
    accepts_loss_kwargs = False
    config: Qwen3VLConfig

    def __init__(self, config):
        super().__init__(config)
        self.model = Qwen3VLModel(config)
        self.lm_head = nn.Linear(config.text_config.hidden_size, config.text_config.vocab_size, bias=False)

        self.post_init()

    def get_input_embeddings(self):
        return self.model.get_input_embeddings()

    def set_input_embeddings(self, value):
        self.model.set_input_embeddings(value)

    def set_decoder(self, decoder):
        self.model.set_decoder(decoder)

    def get_decoder(self):
        return self.model.get_decoder()

    def get_video_features(
        self, pixel_values_videos: torch.FloatTensor, video_grid_thw: Optional[torch.LongTensor] = None
    ):
        return self.model.get_video_features(pixel_values_videos, video_grid_thw)

    def get_image_features(self, pixel_values: torch.FloatTensor, image_grid_thw: Optional[torch.LongTensor] = None):
        return self.model.get_image_features(pixel_values, image_grid_thw)

    # Make modules available through conditional class for BC
    @property
    def language_model(self):
        return self.model.language_model

    @property
    def visual(self):
        return self.model.visual

    @check_model_inputs
    def forward(
        self,
        input_ids: torch.LongTensor = None,
        attention_mask: Optional[torch.Tensor] = None,
        position_ids: Optional[torch.LongTensor] = None,
        past_key_values: Optional[Cache] = None,
        inputs_embeds: Optional[torch.FloatTensor] = None,
        labels: Optional[torch.LongTensor] = None,
        pixel_values: Optional[torch.Tensor] = None,
        pixel_values_videos: Optional[torch.FloatTensor] = None,
        pixel_values_panoramic: Optional[torch.FloatTensor] = None,
        image_grid_thw: Optional[torch.LongTensor] = None,
        video_grid_thw: Optional[torch.LongTensor] = None,
        panoramic_grid_thw: Optional[torch.LongTensor] = None,
        cache_position: Optional[torch.LongTensor] = None,
        logits_to_keep: Union[int, torch.Tensor] = 0,
        panoramic_pos_mask: Optional[torch.Tensor] = None,
        panoramic_width_map: Optional[torch.Tensor] = None,
        **kwargs: Unpack[TransformersKwargs],
    ) -> Union[tuple, Qwen3VLCausalLMOutputWithPast]:
        r"""
        labels (`torch.LongTensor` of shape `(batch_size, sequence_length)`, *optional*):
            Labels for computing the masked language modeling loss. Indices should either be in `[0, ...,
            config.vocab_size]` or -100 (see `input_ids` docstring). Tokens with indices set to `-100` are ignored
            (masked), the loss is only computed for the tokens with labels in `[0, ..., config.vocab_size]`.
        image_grid_thw (`torch.LongTensor` of shape `(num_images, 3)`, *optional*):
            The temporal, height and width of feature shape of each image in LLM.
        video_grid_thw (`torch.LongTensor` of shape `(num_videos, 3)`, *optional*):
            The temporal, height and width of feature shape of each video in LLM.
        panoramic_grid_thw (`torch.LongTensor` of shape `(num_panoramic_images, 3)`, *optional*):
            The temporal, height and width of feature shape of each panoramic image in LLM.

        Example:
            TODO: Add example
        """
        outputs = self.model(
            input_ids=input_ids,
            pixel_values=pixel_values,
            pixel_values_videos=pixel_values_videos,
            pixel_values_panoramic=pixel_values_panoramic,
            image_grid_thw=image_grid_thw,
            video_grid_thw=video_grid_thw,
            panoramic_grid_thw=panoramic_grid_thw,
            position_ids=position_ids,
            attention_mask=attention_mask,
            past_key_values=past_key_values,
            inputs_embeds=inputs_embeds,
            cache_position=cache_position,
            panoramic_pos_mask=panoramic_pos_mask,
            panoramic_width_map=panoramic_width_map,
            **kwargs,
        )

        hidden_states = outputs[0]

        # Only compute necessary logits, and do not upcast them to float if we are not computing the loss
        slice_indices = slice(-logits_to_keep, None) if isinstance(logits_to_keep, int) else logits_to_keep
        logits = self.lm_head(hidden_states[:, slice_indices, :])

        loss = None
        if labels is not None:
            loss = self.loss_function(logits=logits, labels=labels, vocab_size=self.config.text_config.vocab_size)

        return Qwen3VLCausalLMOutputWithPast(
            loss=loss,
            logits=logits,
            past_key_values=outputs.past_key_values,
            rope_deltas=outputs.rope_deltas,
        )

    def prepare_inputs_for_generation(
        self,
        input_ids,
        past_key_values=None,
        attention_mask=None,
        inputs_embeds=None,
        cache_position=None,
        position_ids=None,
        use_cache=True,
        pixel_values=None,
        pixel_values_videos=None,
        pixel_values_panoramic=None,
        image_grid_thw=None,
        video_grid_thw=None,
        panoramic_grid_thw=None,
        panoramic_pos_mask=None,
        panoramic_width_map=None,
        **kwargs,
    ):
        # Overwritten -- in specific circumstances we don't want to forward image inputs to the model

        model_inputs = super().prepare_inputs_for_generation(
            input_ids,
            past_key_values=past_key_values,
            attention_mask=attention_mask,
            inputs_embeds=inputs_embeds,
            cache_position=cache_position,
            position_ids=position_ids,
            pixel_values=pixel_values,
            pixel_values_videos=pixel_values_videos,
            pixel_values_panoramic=pixel_values_panoramic,
            image_grid_thw=image_grid_thw,
            video_grid_thw=video_grid_thw,
            panoramic_grid_thw=panoramic_grid_thw,
            use_cache=use_cache,
            **kwargs,
        )

        # Qwen3VL position_ids are prepareed with rope_deltas in forward
        model_inputs["position_ids"] = None

        if cache_position[0] != 0:
            model_inputs["pixel_values"] = None
            model_inputs["pixel_values_videos"] = None
            model_inputs["pixel_values_panoramic"] = None
            
            # In generation steps after prefill, we need to slice panoramic_pos_mask and panoramic_width_map
            # to match the current token position (usually size 1)
            # For newly generated tokens, panoramic_pos_mask should be False (not a panoramic token)
            if hasattr(self.model, 'panoramic_pos_mask') and self.model.panoramic_pos_mask is not None:
                batch_size = input_ids.shape[0]
                current_seq_len = input_ids.shape[1]
                # Create a mask of zeros (False) for the new tokens
                model_inputs["panoramic_pos_mask"] = torch.zeros(
                    (batch_size, current_seq_len),
                    dtype=torch.bool,
                    device=input_ids.device
                )
                # Create a width map of zeros for the new tokens
                model_inputs["panoramic_width_map"] = torch.zeros(
                    (batch_size, current_seq_len),
                    dtype=torch.long,
                    device=input_ids.device
                )
        else:
            # During prefill (first step), use provided or cached panoramic parameters
            if panoramic_pos_mask is not None:
                model_inputs["panoramic_pos_mask"] = panoramic_pos_mask
            elif hasattr(self.model, 'panoramic_pos_mask') and self.model.panoramic_pos_mask is not None:
                model_inputs["panoramic_pos_mask"] = self.model.panoramic_pos_mask
                
            if panoramic_width_map is not None:
                model_inputs["panoramic_width_map"] = panoramic_width_map
            elif hasattr(self.model, 'panoramic_width_map') and self.model.panoramic_width_map is not None:
                model_inputs["panoramic_width_map"] = self.model.panoramic_width_map

        return model_inputs

    def _get_image_nums_and_video_nums(
        self,
        input_ids: Optional[torch.LongTensor],
        inputs_embeds: Optional[torch.Tensor] = None,
    ) -> tuple[torch.Tensor, torch.Tensor]:
        """
        Get the number of images and videos for each sample to calculate the separation length of the sample tensor.
        These parameters are not passed through the processor to avoid unpredictable impacts from interface modifications.

        Args:
            input_ids (`torch.LongTensor` of shape `(batch_size, sequence_length)`):
                Indices of input sequence tokens in the vocabulary.

        Returns:
            image_nums (`torch.LongTensor` of shape `(batch_size, num_images_sample)`)
            video_nums (`torch.LongTensor` of shape `(batch_size, num_videos_sample)`)
        """
        image_token_id = self.config.image_token_id
        video_token_id = self.config.video_token_id
        vision_start_token_id = self.config.vision_start_token_id

        if inputs_embeds is not None:
            vision_start_mask = (
                inputs_embeds
                == self.get_input_embeddings()(
                    torch.tensor(vision_start_token_id, dtype=torch.long, device=inputs_embeds.device)
                )
            )[..., 0]
            image_mask = (
                inputs_embeds
                == self.get_input_embeddings()(
                    torch.tensor(image_token_id, dtype=torch.long, device=inputs_embeds.device)
                )
            )[..., 0]
            video_mask = (
                inputs_embeds
                == self.get_input_embeddings()(
                    torch.tensor(video_token_id, dtype=torch.long, device=inputs_embeds.device)
                )
            )[..., 0]
        else:
            vision_start_mask = input_ids == vision_start_token_id
            image_mask = input_ids == image_token_id
            video_mask = input_ids == video_token_id

        vision_first_mask = torch.roll(vision_start_mask, shifts=1, dims=1)
        image_nums = torch.sum(vision_first_mask & image_mask, dim=1)
        video_nums = torch.sum(vision_first_mask & video_mask, dim=1)

        return image_nums, video_nums

    def _expand_inputs_for_generation(
        self,
        expand_size: int = 1,
        is_encoder_decoder: bool = False,
        input_ids: Optional[torch.LongTensor] = None,
        **model_kwargs,
    ) -> tuple[torch.LongTensor, dict[str, Any]]:
        # Overwritten -- Support for expanding tensors without a batch size dimension
        # e.g., pixel_values, image_grid_thw, pixel_values_videos, video_grid_thw, second_per_grid_t
        # pixel_values.shape[0] is sum(seqlen_images for samples)
        # image_grid_thw.shape[0] is sum(num_images for samples)

        if expand_size == 1:
            return input_ids, model_kwargs

        visual_keys = ["pixel_values", "image_grid_thw", "pixel_values_videos", "video_grid_thw", "second_per_grid_ts"]

        def _expand_dict_for_generation_visual(dict_to_expand):
            image_grid_thw = model_kwargs.get("image_grid_thw", None)
            video_grid_thw = model_kwargs.get("video_grid_thw", None)
            image_nums, video_nums = self._get_image_nums_and_video_nums(
                input_ids, inputs_embeds=model_kwargs.get("inputs_embeds", None)
            )

            def _repeat_interleave_samples(x, lengths, repeat_times):
                samples = torch.split(x, lengths)
                repeat_args = [repeat_times] + [1] * (x.dim() - 1)
                result = torch.cat([sample.repeat(*repeat_args) for sample in samples], dim=0)
                return result

            for key in dict_to_expand:
                if key == "pixel_values":
                    # split images into samples
                    samples = torch.split(image_grid_thw, list(image_nums))
                    # compute the sequence length of images for each sample
                    lengths = [torch.prod(sample, dim=1).sum() for sample in samples]
                    dict_to_expand[key] = _repeat_interleave_samples(
                        dict_to_expand[key], lengths=lengths, repeat_times=expand_size
                    )
                elif key == "image_grid_thw":
                    # get the num of images for each sample
                    lengths = list(image_nums)
                    dict_to_expand[key] = _repeat_interleave_samples(
                        dict_to_expand[key], lengths=lengths, repeat_times=expand_size
                    )
                elif key == "pixel_values_videos":
                    samples = torch.split(video_grid_thw, list(video_nums))
                    lengths = [torch.prod(sample, dim=1).sum() for sample in samples]
                    dict_to_expand[key] = _repeat_interleave_samples(
                        dict_to_expand[key], lengths=lengths, repeat_times=expand_size
                    )
                elif key == "video_grid_thw":
                    lengths = list(video_nums)
                    dict_to_expand[key] = _repeat_interleave_samples(
                        dict_to_expand[key], lengths=lengths, repeat_times=expand_size
                    )
                elif key == "second_per_grid_ts":
                    dict_to_expand[key] = _repeat_interleave_samples(
                        dict_to_expand[key], lengths=list(video_nums), repeat_times=expand_size
                    )
            return dict_to_expand

        def _expand_dict_for_generation(dict_to_expand):
            for key in dict_to_expand:
                if (
                    key != "cache_position"
                    and dict_to_expand[key] is not None
                    and isinstance(dict_to_expand[key], torch.Tensor)
                    and key not in visual_keys
                ):
                    dict_to_expand[key] = dict_to_expand[key].repeat_interleave(expand_size, dim=0)
            return dict_to_expand

        model_kwargs = _expand_dict_for_generation_visual(model_kwargs)

        if input_ids is not None:
            input_ids = input_ids.repeat_interleave(expand_size, dim=0)

        model_kwargs = _expand_dict_for_generation(model_kwargs)

        if is_encoder_decoder:
            if model_kwargs.get("encoder_outputs") is None:
                raise ValueError("If `is_encoder_decoder` is True, make sure that `encoder_outputs` is defined.")
            model_kwargs["encoder_outputs"] = _expand_dict_for_generation(model_kwargs["encoder_outputs"])

        return input_ids, model_kwargs


__all__ = [
    "Qwen3VLVisionModel",
    "Qwen3VLForConditionalGeneration",
    "Qwen3VLModel",
    "Qwen3VLPreTrainedModel",
    "Qwen3VLTextModel",
]
